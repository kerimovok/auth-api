# =============================================================================
# AUTH API ENVIRONMENT VARIABLES
# =============================================================================

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server port (default: 3001)
PORT=3001

# Environment mode: development or production (default: development)
GO_ENV=development

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL host address
DB_HOST=localhost

# PostgreSQL port (default: 5432)
DB_PORT=5432

# PostgreSQL username
DB_USER=root

# PostgreSQL password
DB_PASS=root

# PostgreSQL database name (default: auth)
DB_NAME=auth

# =============================================================================
# JWT CONFIGURATION
# =============================================================================

# JWT secret key (must be at least 32 characters long)
# Generate a secure random string for production use
JWT_SECRET=your-super-secret-jwt-key-32-chars-long

# =============================================================================
# MAILER CONFIGURATION
# =============================================================================

# Mailer service URI (for sending emails)
# Format: http://hostname:port or https://hostname:port
MAILER_URI=http://localhost:3002

# =============================================================================
# RABBITMQ CONFIGURATION (Optional - for email queue)
# =============================================================================

# RabbitMQ host (default: localhost)
RABBITMQ_HOST=localhost

# RabbitMQ port (default: 5672)
RABBITMQ_PORT=5672

# RabbitMQ username (default: guest)
RABBITMQ_USERNAME=guest

# RabbitMQ password (default: guest)
RABBITMQ_PASSWORD=guest

# RabbitMQ virtual host (default: /)
RABBITMQ_VHOST=/